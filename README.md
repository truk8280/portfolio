# Portfolio

Personal portfolio site for Kurt Kroll showcasing selected work in fantasy sports tooling. It features a hero/about intro, a grid of project cards with screenshots, and a footer with contact links.

## Getting started

```bash
npm install
npm run dev      # start the local dev server
npm run build    # produce a production build in dist/
npm run preview  # preview the production build
```
